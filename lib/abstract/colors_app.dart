/*
*
*
*      Criando consts que são chamadas no app
*
*
 */

import 'package:flutter/cupertino.dart';

class ColorsApp {
  final primaryColor = Color(0xFFFFA451);
  final textParagraph = Color(0xFF5D577E);
  final formBackgroundColor = Color(0xFFF5F5F5);
  final colorTextForm = Color(0xFFC2BDBD);
  final colorThemeAppBar = Color(0xFF27214D);
  final colorValorProduct = Color(0xFFF08626);
  final colorBackCurtida = Color(0xFFFFF7F0);
  final colorBackaAdicionarRemover = Color(0xFFFFF2E7);
  final colorBackGroundItem = Color(0xFFFFFAEB);

}

